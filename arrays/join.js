// takes an array combines them to form one string. It takes one optional argument: a sperator


const arr = ['apple', 'oranges', 'banna']

const str = arr.join(" ");

console.log(str)

const words = ['I', "am", 'hungry']

const sentence = words.join(", ")

console.log(sentence)
