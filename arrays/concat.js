// method is used to merge two or more arrays. This method does not change the existing arrays, but instead returns a new array

let arr = ["dae", 'new haven', 'nyc']

let arr2 = ["mo", 'dom']

let newArr = arr.concat(arr2)

console.log(newArr)


ON PAGE TEN