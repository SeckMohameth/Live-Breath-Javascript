// sorts the array from least to greatest with numeric values and in alphabetical order with strings

const arr = ["apples", "ornage", "grapes", "banna", "berry"]

const num = [1, 5, 3, 8, 2, 7, 4]

console.log(arr.sort())
console.log(num.sort())


const order = [2, "apple", 5, "orange"]

console.log(order.sort)
// returns [Function: sort]