// removes the last element of an array and returns that element 

const arr = ["apple", "orange", "banna"]

const remove = arr.pop()

console.log(remove)

console.log(arr) // output: [ 'apple', 'orange' ]